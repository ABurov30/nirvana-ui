import React from 'react'
import BlockButton from '../../BlockButton/BlockButton'
import Mix from '../../../../Icons/Mix'

export default function MixBlockButton() {
	return <BlockButton text={'Mix'} icon={Mix} />
}
